name: Apply Repository Conventions

on:
  schedule:
  - cron: '47 9 * * 1-5'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  apply:
    uses: LogosBible/actions/.github/workflows/repo-conventions-call.yml@master
    secrets: inherit
