# Release Notes

## Unreleased

* Add support for passing multiple convention paths to `repo-conventions add`.
* Add `repo-conventions add --open-pr`, which commits added convention references, applies conventions, and opens or updates a generated pull request.

## 0.1.x

* Initial releases.
